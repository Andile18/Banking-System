<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiBankingExercise.Views.CustomerSelectionPage"
             Title="Select Customer">
    <VerticalStackLayout Padding="20" Spacing="12">
        <Label Text="Select a Customer" FontSize="24" HorizontalOptions="Center" />

        <CollectionView
            ItemsSource="{Binding Customers}"
            SelectionMode="Single"
            SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    
                        <VerticalStackLayout>
                            <Label Text="{Binding FirstName}" FontAttributes="Bold" />
                            <Label Text="{Binding LastName}" />
                            <Label Text="{Binding Email}" FontSize="12" Opacity="0.7" />
                        </VerticalStackLayout>
                    
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>